## OpenIMIS App Tracker

This modules tracks app events using a library called django-easy-audit to retrieve:

* Request Events
* CRUD Events
* Login Events

Requirements are:

```
django>=3.2
django-easy-audit==1.3.6
```
